---
const posts = Object.values(import.meta.glob('./posts/*.md', { eager: true })) as {
  frontmatter: { title: string; date: string };
  url: string;
}[];

---

<p>Post Archive:</p>
<ul>
  {posts.map(post => <li><a href={post.url}>{post.frontmatter.title}</a></li>)}
</ul>